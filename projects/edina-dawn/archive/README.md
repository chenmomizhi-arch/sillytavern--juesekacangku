# Archive

This directory stores retired versions and discarded image iterations. Archived files are backups only and must not be referenced by the active card.

- `v1.0.7/`, `v1.0.8/`: retired release sets.
- `v1.0.9/`: raw generation candidates and review iterations for the current active portrait set.

When replacing an active portrait, preserve the old copy here before changing `portraits/`.
