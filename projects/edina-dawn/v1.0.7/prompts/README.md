# Candidate Prompt Notes

The machine-readable source is `candidates.json`.

Prompt rules:

1. Use the project worldbook as the authority for appearance, clothing, role, and personality.
2. Keep all portraits non-explicit.
3. Do not add age status, age numbers, age verification, developmental-age wording, or age-coded body claims.
4. Keep the same camera, lighting, quality, and negative prompt across all eight characters.
5. Store the final seed and output hash in `manifest.json` after selection.
